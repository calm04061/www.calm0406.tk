import { Button } from '@mui/material';
export default function About() {
    return (<div>
        <h2>About</h2>
        <Button variant="contained">
            Button
        </Button>
    </div>);
}