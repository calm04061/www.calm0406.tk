import { Button } from '@mui/material';
export default function Home() {
    return (<div>
        <h2>Home</h2>
        <Button variant="contained">
            Button
        </Button>
    </div>);
}