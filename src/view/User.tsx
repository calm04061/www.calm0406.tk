import { Button } from '@mui/material';
export default function User() {
    return (<div>
        <h2>User</h2>
        <Button variant="contained">
            Button
        </Button>
    </div>);
}